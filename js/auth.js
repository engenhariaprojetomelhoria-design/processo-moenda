import { auth, db, signInWithEmailAndPassword, onAuthStateChanged, doc, getDoc, setDoc, serverTimestamp } from "./firebase.js";
const emailInput=document.getElementById("email"), passwordInput=document.getElementById("password"), loginBtn=document.getElementById("loginBtn"), loginMsg=document.getElementById("loginMsg");
onAuthStateChanged(auth,(user)=>{if(user) window.location.href="dashboard.html";});
loginBtn.addEventListener("click",async()=>{const email=emailInput.value.trim(), password=passwordInput.value.trim(); if(!email||!password){loginMsg.innerHTML='<div class="alert">Informe e-mail e senha.</div>';return;} try{const result=await signInWithEmailAndPassword(auth,email,password);const user=result.user;const ref=doc(db,"usuarios",user.uid);const snap=await getDoc(ref);if(!snap.exists()){await setDoc(ref,{email:user.email,perfil:"administrador",nome:user.email,criadoEm:serverTimestamp()});} window.location.href="dashboard.html";}catch(e){loginMsg.innerHTML=`<div class="alert">Erro ao entrar: ${e.message}</div>`;}});